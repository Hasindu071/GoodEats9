<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Button fill color -->
            <stroke android:width="2dp" android:color="@color/green" /> <!-- Border color and width -->
            <corners android:radius="4dp" /> <!-- Optional: For rounded corners -->
        </shape>
    </item>
</selector>